function draw{color, shape(
  local color = [color]
  local shape = [shape]
  local scene = [scene]
)};

function scene{(
  local bgc = [color]
  global s# = [number]
)};



local rend = draw.color.shape
global scene = scene.color.number
