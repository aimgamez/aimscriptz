function draw{color, shape(
  local color = [color]
  local shape = [shape]
  local scene = [scene]
)};

function scene{bgc, s#(
  local bgc = [color]
  global s# = [number]
)};

function setScene{num(
  global s# = setScene.number
)}

function 3D{(
  print.("Bruh")
)}

local rend = draw.color.shape
global scene = scene.color/image.number
